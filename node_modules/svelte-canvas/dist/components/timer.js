import { readable } from 'svelte/store';
let frame;
const now = Date.now();
const start = (set) => {
    set(Date.now() - now);
    frame = window.requestAnimationFrame(() => start(set));
    return () => window.cancelAnimationFrame(frame);
};
function noop() {
    void 0;
}
export const timer = readable(Date.now() - now, typeof window === 'undefined' ? noop : start);
